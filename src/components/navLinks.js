import React from "react";

const NavLinks = () => {
  return (
    <div className="flex space-x-6 text-gray-700  ">
      <a href="/" className="hover:text-black hover:font-bold font-semibold">Stays</a>
      <a href="/" className="hover:text-black hover:font-bold font-semibold">Experiences</a>
    </div>
  );
};

export default NavLinks;
